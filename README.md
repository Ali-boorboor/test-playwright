## Run tests:

```bash
pnpm exec playwright test
```

## View tests with (html file):

```bash
pnpm exec playwright show-report --host=127.0.0.1
```

## Run/View tests with (UI):

```bash
pnpm exec playwright test --ui --ui-host=127.0.0.1
```
